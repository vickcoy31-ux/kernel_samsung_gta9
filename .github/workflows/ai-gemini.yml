- name: Inject KernelSU Clean Setup
        run: |
          echo "=== 1. Bersihkan KSU lama ==="
          rm -rf drivers/kernelsu
          sed -i '/kernelsu/d' drivers/Kconfig
          sed -i '/kernelsu/d' drivers/Makefile
          
          echo "=== 2. Clone KernelSU ==="
          git clone --depth=1 https://github.com/tiann/KernelSU.git drivers/kernelsu || true
          
          echo "=== 3. Daftarkan Kconfig & Makefile ==="
          if [ -d "drivers/kernelsu" ]; then
            echo 'source "drivers/kernelsu/Kconfig"' >> drivers/Kconfig
            echo 'obj-y += kernelsu/' >> drivers/Makefile
            echo "KernelSU berhasil di-inject!"
          else
            echo "Gagal mengunduh KernelSU!"
            exit 1
          fi
